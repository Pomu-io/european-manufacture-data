{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Scrapy Crawl",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run.py",
      "console": "integratedTerminal",
      "args": ["scrapy", "crawl", "productLinksSpider"],
      "python": "${workspaceFolder}/myenv/bin/python", // Explicit path to virtual environment interpreter
      "justMyCode": true
    }
  ]
}
